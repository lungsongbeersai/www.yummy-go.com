import type { LocationRow } from "./location-utils";

export type LocationSettingsRow = LocationRow;

export interface LocationLabels {
  district: string;
  no: string;
  province: string;
  sortAsc: string;
  sortDesc: string;
}

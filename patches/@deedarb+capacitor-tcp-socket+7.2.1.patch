diff --git a/node_modules/@deedarb/capacitor-tcp-socket/android/build.gradle b/node_modules/@deedarb/capacitor-tcp-socket/android/build.gradle
index 3d335a6..4062c71 100644
--- a/node_modules/@deedarb/capacitor-tcp-socket/android/build.gradle
+++ b/node_modules/@deedarb/capacitor-tcp-socket/android/build.gradle
@@ -30,7 +30,7 @@ android {
     buildTypes {
         release {
             minifyEnabled false
-            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
+            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
         }
     }
     lintOptions {
diff --git a/node_modules/@deedarb/capacitor-tcp-socket/android/src/main/java/com/svend/plugins/tcp/socket/TcpSocketPlugin.java b/node_modules/@deedarb/capacitor-tcp-socket/android/src/main/java/com/svend/plugins/tcp/socket/TcpSocketPlugin.java
index 78ec7c8..5d53865 100644
--- a/node_modules/@deedarb/capacitor-tcp-socket/android/src/main/java/com/svend/plugins/tcp/socket/TcpSocketPlugin.java
+++ b/node_modules/@deedarb/capacitor-tcp-socket/android/src/main/java/com/svend/plugins/tcp/socket/TcpSocketPlugin.java
@@ -19,7 +19,9 @@ import java.io.IOException;
 import java.net.Socket;
 import java.net.InetSocketAddress;
 import java.util.ArrayList;
+import java.util.Arrays;
 import java.util.Base64;
+import java.util.Collections;
 import java.util.List;
 
 @CapacitorPlugin(name = "TcpSocket", permissions = {
@@ -30,9 +32,26 @@ import java.util.List;
 })
 public class TcpSocketPlugin extends Plugin {
 
-    private Socket socket;
-    private DataOutputStream mBufferOut;
-    private List<Socket> clients = new ArrayList<>();
+    private final List<Socket> clients = Collections.synchronizedList(new ArrayList<>());
+    private final List<DataOutputStream> outputs = Collections.synchronizedList(new ArrayList<>());
+
+    private Socket getClient(int client) {
+        synchronized (clients) {
+            if (client < 0 || client >= clients.size()) {
+                return null;
+            }
+            return clients.get(client);
+        }
+    }
+
+    private DataOutputStream getOutput(int client) {
+        synchronized (outputs) {
+            if (client < 0 || client >= outputs.size()) {
+                return null;
+            }
+            return outputs.get(client);
+        }
+    }
 
     @PluginMethod()
     public void connect(PluginCall call) {
@@ -45,21 +64,29 @@ public class TcpSocketPlugin extends Plugin {
         Integer port = call.getInt("port", 9100);
         Integer timeout = call.getInt("timeout", 10); // Default 10 second timeout (in seconds)
 
+        int clientIndex;
+        Socket clientSocket = new Socket();
         try {
-            if (socket != null && socket.isConnected()) {
-                socket.close();
+            clientSocket.connect(new InetSocketAddress(ipAddress, port), timeout * 1000); // Convert seconds to milliseconds
+            DataOutputStream output = new DataOutputStream(new BufferedOutputStream(clientSocket.getOutputStream()));
+            synchronized (clients) {
+                clientIndex = clients.size();
+                clients.add(clientSocket);
+                outputs.add(output);
             }
-            socket = new Socket();
-            socket.connect(new InetSocketAddress(ipAddress, port), timeout * 1000); // Convert seconds to milliseconds
-            clients.add(socket);
         } catch (IOException e) {
+            try {
+                clientSocket.close();
+            } catch (IOException ignored) {
+                // Preserve the original connection error.
+            }
             Log.d("Connection failed", e.getMessage());
             call.reject(e.getMessage());
             return;
         }
 
         JSObject ret = new JSObject();
-        ret.put("client", clients.size() - 1);
+        ret.put("client", clientIndex);
         call.resolve(ret);
     }
 
@@ -73,30 +100,29 @@ public class TcpSocketPlugin extends Plugin {
             return;
         }
 
+        final Socket clientSocket = getClient(client);
+        final DataOutputStream output = getOutput(client);
+        if (clientSocket == null || output == null || clientSocket.isClosed()) {
+            call.reject("Invalid client or socket closed");
+            return;
+        }
+
         Runnable runnable = () -> {
             try {
                 if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
-                    final Socket socket = clients.get(client);
-                    mBufferOut = new DataOutputStream(new BufferedOutputStream(socket.getOutputStream()));
                     byte[] decoded = Base64.getDecoder().decode(msg);
-                    if (mBufferOut != null) {
-                        mBufferOut.write(decoded);
-                        mBufferOut.flush();
+                    synchronized (output) {
+                        output.write(decoded);
+                        output.flush();
                     }
                 }
                 call.resolve();
-            } catch (IOException e) {
+            } catch (IOException | IllegalArgumentException e) {
                 call.reject(e.getMessage());
             }
         };
 
-        Socket socket = clients.get(client);
-        if (!socket.isConnected()) {
-            try {
-                socket.close();
-            } catch (IOException e) {
-                call.reject("Generic error");
-            }
+        if (!clientSocket.isConnected()) {
             call.reject("Socket not connected");
             return;
         }
@@ -108,7 +134,7 @@ public class TcpSocketPlugin extends Plugin {
     public void read(final PluginCall call) {
         final Integer client = call.getInt("client", -1);
         final Integer length = call.getInt("expectLen", 1024);
-
+        final Integer timeout = call.getInt("timeout", 10);
         if (client == -1 || length == -1) {
             call.reject("Client or length not specified");
             return;
@@ -118,12 +144,16 @@ public class TcpSocketPlugin extends Plugin {
             try {
                 if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
                     final Socket socket = clients.get(client);
+                    socket.setSoTimeout(Math.max(1, timeout) * 1000);
                     DataInputStream mBufferIn = new DataInputStream(new BufferedInputStream(socket.getInputStream()));
                     byte[] bytes = new byte[length];
                     int read = mBufferIn.read(bytes, 0, length);
-                    Base64.getEncoder().encodeToString(bytes);
+                    if (read < 0) {
+                        call.reject("Socket closed before data was received");
+                        return;
+                    }
                     JSObject ret = new JSObject();
-                    ret.put("result", new String(bytes, 0, read));
+                    ret.put("result", Base64.getEncoder().encodeToString(Arrays.copyOf(bytes, read)));
                     call.resolve(ret);
                 } else {
                     JSObject ret = new JSObject();
@@ -135,13 +165,8 @@ public class TcpSocketPlugin extends Plugin {
             }
         };
 
-        Socket socket = clients.get(client);
-        if (!socket.isConnected()) {
-            try {
-                socket.close();
-            } catch (IOException e) {
-                call.reject("Generic error");
-            }
+        Socket socket = getClient(client);
+        if (socket == null || socket.isClosed() || !socket.isConnected()) {
             call.reject("Socket not connected");
             return;
         }
@@ -156,19 +181,36 @@ public class TcpSocketPlugin extends Plugin {
             call.reject("No client specified");
             return;
         }
-        if (clients.isEmpty()) {
-            call.reject("Socket not connected");
+        final Socket socket = getClient(client);
+        final DataOutputStream output = getOutput(client);
+        if (socket == null) {
+            call.reject("Invalid client index");
             return;
         }
-        final Socket socket = clients.get(client);
+        IOException closeError = null;
         try {
-            if (!socket.isConnected()) {
-                socket.close();
-                call.reject("Socket not connected");
+            if (output != null) {
+                output.close();
             }
-            socket.close();
         } catch (IOException e) {
-            call.reject(e.getMessage());
+            closeError = e;
+        }
+        try {
+            if (!socket.isClosed()) socket.close();
+        } catch (IOException e) {
+            if (closeError == null) closeError = e;
+        }
+
+        synchronized (clients) {
+            clients.set(client, null);
+        }
+        synchronized (outputs) {
+            outputs.set(client, null);
+        }
+
+        if (closeError != null) {
+            call.reject(closeError.getMessage());
+            return;
         }
 
         JSObject ret = new JSObject();
